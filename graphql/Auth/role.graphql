type Role {
    id: ID!
    name: String!
    remark: String
    created_at: DateTime
    updated_at: DateTime
}

input RoleInput {
    name: String! @rules(apply: ["unique:roles,name"])
    remark: String
}

extend type Mutation {
    insert_role_one(input: RoleInput! @spread): Role! @create
}
