type Permission{
    name: String!
    created_at: DateTime
    updated_at: DateTime
    role: [Role!]! @belongsToMany
}

# extend type Mutation
# {
#     insert_permission_one(name: String! @rules(apply: ["required"])): Permission! @create
#     update_permission_one(id: ID!, name: String!): Permission! @update

# }