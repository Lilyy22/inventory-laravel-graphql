extend type Mutation
{
    send_password_reset_token(email: String!): Message!
        @field(resolver: "App\\GraphQL\\Mutations\\Send_password_reset_token")

    reset_password(token: String!): Message!
        @field(resolver: "App\\GraphQL\\Mutations\\Reset_password")
    
    change_password(newPassword: String!, confirmPassword: String!): Message!
        @field(resolver: "App\\GraphQL\\Mutations\\Change_password")
}